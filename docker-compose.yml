services:
  db:
    image: postgres
    environment:
      - POSTGRES_USER=pokedex
      - POSTGRES_DB=pokedex
      - POSTGRES_PASSWORD=pokedex
    ports:
      - 5342:5432
    volumes:
      - db_data:/var/lib/postgresql

volumes:
  db_data:
